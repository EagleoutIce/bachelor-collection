\lstdefinelanguage{lPseudo}{
    morestring=[s]"", morestring=[s]'',
    keywordstyle = [1]\bfseries,
    keywordstyle = [2]\bfseries,
    keywordstyle = [3]\bfseries\itshape,
    keywordstyle = [4]\bfseries,
    alsoother={.-},
    sensitive=true,
    morekeywords = {children,node,replace,remove,from,with,value,random,domain,return,break,subtree}, % keywords
    morekeywords = [2]{is,are,has,have,filter,as,equals}, % utility
    morekeywords = [3]{leaf,leafs,known,constant,function,functions,finite,signature,reference}, % predicates
    morekeywords = [4]{and, or, xor, not},
    add to literate={%
        {<}{{{$\langle$}}}2 {>}{{{$\rangle$}}}2
        {/}{{{\thinspace\textbf{/}\thinspace}}}2
    }
}

\RegisterLanguage{pseudo}{lPseudo}